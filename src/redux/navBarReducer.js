let initialState = {
      friendsData: [
      { id: 1, name: 'friend 1' },
      { id: 2, name: 'friend 2' },
      { id: 3, name: 'friend 3' }
    ]  
} 

const navBarReducer = (state = initialState, action) => {
return state
}

export default navBarReducer